#include <iostream>
using namespace std;
int main()
{
    // STL stands for Standard Template Library
    // It is a powerful set of C++ template classes to provide general-purpose classes and functions with templates.
    // It includes algorithms and data structures that allow programmers to use them without having to write them from scratch.

    // The main components of STL are:
    // 1. Containers: These are data structures that store objects and data. Examples include vector, list, deque, set, map, etc.
    // 2. Algorithms: These are a collection of functions that perform operations on containers. Examples include sort, find, copy, etc.
    // 3. Iterators: These are objects that point to elements in a container and allow traversal through the container. They act as a bridge between containers and algorithms.

    // Why we will use STL?
    // 1. Efficiency: STL provides efficient implementations of data structures and algorithms, which can save time and effort for programmers.
    // 2. Reusability: STL allows programmers to reuse code, as the same algorithms can be applied to different types of containers.
    // 3. Flexibility: STL provides a wide range of data structures and algorithms, allowing programmers to choose the most appropriate one for their needs.

    return 0;
}
#include <stdio.h>
int main()
{
    printf("Hello,\n");
    printf("World!");
    return 0; // without return 0 will work properly. But, it's a good practice to include it. (return 0 means successful termination)
}
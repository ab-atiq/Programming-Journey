/*
________________________
|                      |
|     AB Atiq          |
|______________________|
*/

#include <bits/stdc++.h>
using namespace std;
int main()
{
    new int;               // dynamic memory allocation for integer type
    new int(5);            // dynamic memory allocation with initialization
    new int[10];           // dynamic memory allocation for array of integer type
    int *arr = new int[5]; // return pointer of first index of array. So, we can store it in pointer. int *arr here int is pointer dat type and arr is pointer name.

    return 0;
}